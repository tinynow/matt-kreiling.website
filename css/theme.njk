---
permalink: "css/theme.css"
---

:root {
  --colorLight: {{ tokens.palette["open-boreal"] }};
  --colorDark: {{ tokens.colorDark }};
  {% for name, color in tokens.palette %}--{{ name }}: {{ color }};
{% endfor %}
  {% for name, color in tokens.colors %}--{{ name }}: {{ color }};
{% endfor %}
}

 {% for name, color in tokens.palette %}
.bg--{{ name }} {
    background-color: var(--{{ name }});
}
{% endfor %}
 {% for name, color in tokens.palette %}
.color--{{ name }} {
    color: var(--{{ name }});
}
{% endfor %}